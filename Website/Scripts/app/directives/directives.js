// Define you directives here. Directives can be added to same module as 'main' or a seperate module can be created.

var reportDirectives = angular.module('report.directives', []);     //Define the directive module

//reportDirectives.directive("tablerow", ['$compile', '$http', '$templateCache', function ($compile, $http, $templateCache) {
//    var templateHtml = '',

        
//        return;
//    }]);
